version: v1
type: rule-type
name: vuln_check
display_name: Detect vulnerable dependencies

description: |
  This rule checks if newly added dependencies contain known vulnerabilities.

def:
  in_entity: pull_request

  ingest:
    type: diff

  eval:
    type: vulncheck
